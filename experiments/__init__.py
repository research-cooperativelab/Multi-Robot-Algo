"""SearchFCR experiments package.

Each module in this package runs one thesis-level experiment against the
primitives in `main.py`. Experiments are deliberately independent of each
other: they import from main.py, they produce one figure + one CSV each,
and they can all be regenerated by `python reproduce_thesis.py` from the
repo root.
"""
